---
샘플명: 04_blockquote_code
이전: 03_ordered_list
도입 요소:
  - blockquote
  - code_block
스타일 매핑:
  "> " 로 시작하는 블록:        인용
  "```...```" 펜스드 코드 블록: 코드
테스트 관점:
  - 인용 블록 내부 다중 줄 처리 방식이 일관적인가
    (줄마다 인용 스타일 부여 방식으로 고정. 마크다운 원문의 한 줄 = hwpx 의 한 문단)
  - 언어 지정이 있는 코드 블록("```python") 과 없는 코드 블록 모두 "코드" 스타일로 매핑되는가
  - 코드 블록 내부의 공백, 들여쓰기, 빈 줄, 특수 문자가 원문 그대로 보존되는가
  - 코드 블록 내부의 백틱(`) 이 본문 텍스트로 취급되고 펜스 종료로 오인되지 않는가
---

평문 단락입니다.

> 인용 블록은 본문에서 분리되어 다른 화자나 문헌의 목소리를 드러냅니다.
> 일반적으로 좌우 여백을 들이고 경계선이나 배경으로 본문과 구분합니다.

```python
def 스타일ID(이름):
    return 이름에서ID[이름]
```

언어 지정 없는 코드 블록입니다.

```
hello world
```
